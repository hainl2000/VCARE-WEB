import { DetailFormProps } from "@/type/common.interface";
import React from "react";

const DetailServiceForm = ({
  open,
  setOpen,
  refresh,
  id,
}: DetailFormProps) => {
  return <div>DetailServiceForm</div>;
};

export default DetailServiceForm;
